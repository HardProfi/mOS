local cpm = require("component")
local gpu = cpm.gpu
local os = require("os")
local Ws,Hs = gpu.getResolution()
local math = require("math")
local string = require("string")
local welcomeText = "Загрузка mOS"

--gpu.setResolution(40,40)
gpu.set(Ws/2-6,1, welcomeText)
os.sleep(2)
